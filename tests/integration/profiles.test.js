const request = require('supertest');
const { app } = require('../../src/index');

describe('/api/profiles', () => {
  beforeEach(() => {
  });

  afterEach(() => {
  });

  describe('GET /api/profiles/getRestaurants', () => {
    it('should return a status of 200 when a new user is created', (done) => {

    });
  });
});
